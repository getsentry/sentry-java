CREATE TABLE song (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  title TEXT NOT NULL,
  artist TEXT NOT NULL
);

insertSong:
INSERT INTO song(title, artist)
VALUES (?, ?);

selectAll:
SELECT *
FROM song;

countSongs:
SELECT count(*)
FROM song;
